<ui:FluentWindow xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        x:Class="SText.Dialogs.PasswordDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SText.Dialogs"
        mc:Ignorable="d"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Title="Type a password" Height="180" Width="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="100"/>
        </Grid.RowDefinitions>
        <Grid Height="40" Grid.Row="0">
            <Label x:Name="Header" Margin="15,0,0,0" VerticalAlignment="Center" VerticalContentAlignment="Center" Content="Type a password"/>
        </Grid>
        <Label Grid.Row="1" Content="Please, type a password to encrypt/decrypt current file."/>
        <Grid Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Label Grid.Row="0" Grid.Column="0" Content="Type a password: "/>
            <Label x:Name="RetypedPasswdLabel" Grid.Row="1" Grid.Column="0" Content="Retype a password: "/>
            <ui:TextBox x:Name="Passwd" Grid.Row="0" Grid.Column="1"/>
            <ui:TextBox x:Name="RetypedPasswd" Grid.Row="1" Grid.Column="1"/>
        </Grid>
        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <ui:Button x:Name="OK" Grid.Column="0" Width="100" HorizontalAlignment="Center" VerticalAlignment="Center" Content="OK" Click="Buttons_Click" />
            <ui:Button x:Name="Cancel" Grid.Column="1" Width="100" HorizontalAlignment="Center" VerticalAlignment="Center" Content="Cancel" Click="Buttons_Click"/>
        </Grid>
    </Grid>
</ui:FluentWindow>
