<ui:FluentWindow  xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml" x:Class="SText.Dialogs.SaveDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SText.Dialogs"
        mc:Ignorable="d"
                  MinHeight="0"
                  MinWidth="0"
        WindowStartupLocation="CenterOwner"
        Title="SaveDialog" Height="150" Width="447"
        ResizeMode="NoResize">

    <ui:FluentWindow.Resources>
        <ResourceDictionary>
            <!--<BitmapImage x:Key="QuestionImage"/>-->
        </ResourceDictionary>
    </ui:FluentWindow.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        <!--<ui:TitleBar x:Name="Header" Grid.Row="0"/>-->
        <Grid Height="40" Grid.Row="0">
            <Label x:Name="Header" Margin="15,0,0,0" VerticalAlignment="Center" VerticalContentAlignment="Center" Content="SText"/>
        </Grid>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="64"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Image x:Name="QuestionIcon" Grid.Column="0" Width="48" Height="48"/>
            <Label x:Name="Message" Grid.Column="1" Content="Do you want to save changes to"
                   HorizontalContentAlignment="Left" HorizontalAlignment="Left" 
                   VerticalAlignment="Center" VerticalContentAlignment="Center"/>
        </Grid>
        <DockPanel Grid.Row="2">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <ui:Button x:Name="SaveButton" Grid.Column="0" Content="Save" Width="120" HorizontalAlignment="Right" Click="ActionButtons_Click"/>
                <ui:Button x:Name="AbortButton" Grid.Column="1" Content="Don't save" Width="120" HorizontalAlignment="Center" Click="ActionButtons_Click"/>
                <ui:Button x:Name="CancelButton" Grid.Column="2" Content="Cancel" Width="120" HorizontalAlignment="Left" Click="ActionButtons_Click"/>
            </Grid>
        </DockPanel>
    </Grid>
</ui:FluentWindow>
