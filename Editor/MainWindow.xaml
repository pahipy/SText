<ui:FluentWindow x:Class="SText.Editor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:local="clr-namespace:SText.Editor"
        xmlns:vm="clr-namespace:SText.Editor"
        mc:Ignorable="d"
        Title="SText" Height="450" Width="800"
        Loaded="MainWindow_Loaded"
        Closing="FluentWindow_Closing">
    <ui:FluentWindow.Resources>
        <Style TargetType="Label" BasedOn="{StaticResource {x:Type Label}}" x:Key="BottomLables">
            <Setter Property="Margin" Value="10,0,0,0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <RoutedUICommand x:Key="NewFileCommand" Text="NewFile" />
        <RoutedUICommand x:Key="OpenFileCommand" Text="OpenFile" />
        <RoutedUICommand x:Key="SaveFileCommand" Text="SaveFile" />
        <RoutedUICommand x:Key="SaveAsFileCommand" Text="SaveAsFile" />
        <RoutedUICommand x:Key="PrintFileCommand" Text="PrintFile" />
        <RoutedUICommand x:Key="DateTimeEditCommand" Text="PrintDateTime" />
        <RoutedUICommand x:Key="AlwaysOnTopCommand" Text="Topmost" />
    </ui:FluentWindow.Resources>
    <ui:FluentWindow.CommandBindings>
        <CommandBinding Command="{StaticResource NewFileCommand}" Executed="NewFileCommand"/>
        <CommandBinding Command="{StaticResource OpenFileCommand}" Executed="OpenFileCommand"/>
        <CommandBinding Command="{StaticResource SaveFileCommand}" Executed="SaveFileCommand"/>
        <CommandBinding Command="{StaticResource SaveAsFileCommand}" Executed="SaveAsFileCommand"/>
        <CommandBinding Command="{StaticResource PrintFileCommand}" Executed="PrintFileCommand"/>
        <CommandBinding Command="{StaticResource DateTimeEditCommand}" Executed="DateTimeEditCommand"/>
        <CommandBinding Command="{StaticResource AlwaysOnTopCommand}" Executed="TopmostMenuItemCommand"/>
    </ui:FluentWindow.CommandBindings>
    <Window.InputBindings>
        <KeyBinding Key="N" Modifiers="Ctrl" Command="{StaticResource NewFileCommand}"/>
        <KeyBinding Key="O" Modifiers="Ctrl" Command="{StaticResource OpenFileCommand}"/>
        <KeyBinding Key="S" Modifiers="Ctrl" Command="{StaticResource SaveFileCommand}"/>
        <KeyBinding Key="S" Modifiers="Ctrl+Shift" Command="{StaticResource SaveAsFileCommand}"/>
        <KeyBinding Key="P" Modifiers="Ctrl" Command="{StaticResource PrintFileCommand}"/>
        <KeyBinding Key="F5" Command="{StaticResource DateTimeEditCommand}"/>
        <KeyBinding Key="F2" Command="{StaticResource AlwaysOnTopCommand}"/>
    </Window.InputBindings>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <ui:TitleBar x:Name="TitleText" Grid.Row="0" Title="SText"/>
        <Menu Grid.Row="1">
            <MenuItem Header="File">
                <MenuItem x:Name="New_MenuItem" Header="New" Click="MenuFile_Events_Click" InputGestureText="Ctrl+N"/>
                <MenuItem x:Name="Open_MenuItem" Header="Open" Click="MenuFile_Events_Click" InputGestureText="Ctrl+O"/>
                <MenuItem x:Name="Save_MenuItem" Header="Save" Click="MenuFile_Events_Click" InputGestureText="Ctrl+S"/>
                <MenuItem x:Name="SaveAs_MenuItem" Header="Save As..." Click="MenuFile_Events_Click" InputGestureText="Ctrl+Shift+S"/>
                <Separator/>
                <MenuItem x:Name="Print_MenuItem" Header="Print" Click="MenuFile_Events_Click" InputGestureText="Ctrl+P"/>
                <Separator/>
                <MenuItem x:Name="Exit_MenuItem" Header="Exit" Click="MenuFile_Events_Click"/>
            </MenuItem>
            <MenuItem Header="Edit">
                <MenuItem x:Name="Undo_MenuItem" Header="Undo" InputGestureText="Ctrl+Z" Click="MenuEdit_Events_Click"/>
                <Separator/>
                <MenuItem x:Name="Cut_MenuItem" Header="Cut" InputGestureText="Ctrl+X" Click="MenuEdit_Events_Click"/>
                <MenuItem x:Name="Copy_MenuItem" Header="Copy" InputGestureText="Ctrl+C" Click="MenuEdit_Events_Click"/>
                <MenuItem x:Name="Paste_MenuItem" Header="Paste" InputGestureText="Ctrl+V" Click="MenuEdit_Events_Click"/>
                <MenuItem x:Name="Delete_MenuItem" Header="Delete" InputGestureText="Delete" Click="MenuEdit_Events_Click"/>
                <Separator/>
                <MenuItem x:Name="SelectAll_MenuItem" Header="Select All" InputGestureText="Ctrl+A" Click="MenuEdit_Events_Click"/>
                <MenuItem x:Name="DateTime_MenuItem" Header="Date/Time" InputGestureText="F5" Click="MenuEdit_Events_Click"/>
            </MenuItem>
            <MenuItem Header="Format">
                <MenuItem x:Name="WordWrap_MenuItem" Header="Word Wrap" IsCheckable="True" Click="WordWrap_MenuItem_Click"/>
                <MenuItem x:Name="ChangeFont_MenuItem" Header="Font..." Click="ChangeFont_MenuItem_Click"/>
            </MenuItem>
            <MenuItem Header="View">
                <MenuItem x:Name="AlwaysOnTop_MenuItem" Header="Always on top" IsCheckable="True" InputGestureText="F2" Click="AlwaysOnTop_MenuItem_Click"/>
            </MenuItem>
            <MenuItem Header="Tools">
                <MenuItem Header="Encrypt current file" IsEnabled="False" InputGestureText="Ctrl+E"/>
            </MenuItem>
            <MenuItem Header="Help">
                <MenuItem Header="About SText"/>
            </MenuItem>
        </Menu>
        <TextBox x:Name="ContentViewer" Grid.Row="2" AcceptsReturn="True" AcceptsTab="True" BorderThickness="0"/>

        <Grid Height="25" Grid.Row="3">
            <StackPanel VerticalAlignment="Center" Orientation="Horizontal">
                <Label Content="Theme: Dark" Style="{StaticResource BottomLables}"/>
                <Label x:Name="StatusBar_File" Content="File: " Style="{StaticResource BottomLables}"/>
            </StackPanel>
            <ComboBox x:Name="DropDownEncodingMenu" HorizontalAlignment="Right"></ComboBox>
        </Grid>
    </Grid>
</ui:FluentWindow>
