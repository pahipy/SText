<ui:FluentWindow x:Class="SText.Editor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:local="clr-namespace:SText.Editor"
        xmlns:vm="clr-namespace:SText.Editor"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <ui:FluentWindow.DataContext>
        <vm:MainViewModel/>
    </ui:FluentWindow.DataContext>
    <ui:FluentWindow.Resources>
        <Style TargetType="Label" BasedOn="{StaticResource {x:Type Label}}" x:Key="BottomLables">
            <Setter Property="Margin" Value="10,0,0,0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </ui:FluentWindow.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <ui:TitleBar Grid.Row="0" x:Name="Title" Title="SText"/>
        <Menu Grid.Row="1">
            <MenuItem Header="File">
                <MenuItem Header="New"/>
                <MenuItem Header="Open"/>
                <MenuItem Header="Save"/>
                <MenuItem Header="Save As..."/>
                <Separator/>
                <MenuItem Header="Print"/>
                <Separator/>
                <MenuItem Header="Exit"/>
            </MenuItem>
            <MenuItem Header="Edit">
                <MenuItem Header="Undo"/>
                <Separator/>
                <MenuItem Header="Cut"/>
                <MenuItem Header="Copy"/>
                <MenuItem Header="Paste"/>
                <MenuItem Header="Delete"/>
                <Separator/>
                <MenuItem Header="Select All"/>
                <MenuItem Header="Date/Time"/>
            </MenuItem>
            <MenuItem Header="Format">
                <MenuItem Header="Word Wrap" IsCheckable="True"/>
                <MenuItem Header="Font..."/>
            </MenuItem>
            <MenuItem Header="View">
                <MenuItem Header="Status Bar" IsCheckable="True"/>
                <MenuItem Header="Always on top" IsCheckable="True"/>
            </MenuItem>
            <MenuItem Header="Tools">
                <MenuItem Header="Encrypt current file" IsEnabled="False"/>
            </MenuItem>
            <MenuItem Header="Help">
                <MenuItem Header="About SText" Command="{Binding About}"/>
            </MenuItem>
        </Menu>
        <ui:RichTextBox Language="" Grid.Row="2" AcceptsReturn="True" AcceptsTab="True" BorderThickness="0"/>

        <Grid Height="25" Grid.Row="3">
            <StackPanel VerticalAlignment="Center" Orientation="Horizontal">
                <Label Content="Theme: Dark" Style="{StaticResource BottomLables}"/>
                <Label Content="File: " Style="{StaticResource BottomLables}"/>
            </StackPanel>
            <ComboBox HorizontalAlignment="Right" IsReadOnly="True"></ComboBox>
        </Grid>
    </Grid>
</ui:FluentWindow>
